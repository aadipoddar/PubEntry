<Page x:Class="PubEntryWPF.Transaction.SlipIdPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:PubEntryWPF.Transaction"
	  Loaded="Page_Loaded">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Label Grid.Row="0" Grid.Column="0" Margin="10" Padding="5" Content="Slip Id" />
		<TextBox x:Name="slipIdTextBox" Grid.Row="0" Grid.Column="1" Margin="10" Padding="5" MinWidth="100" PreviewTextInput="textBox_PreviewTextInput" />
		<Button x:Name="loadButton" Content="Load Transaction" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10" Padding="5" MinWidth="150" MinHeight="40" HorizontalAlignment="Center" Style="{DynamicResource AccentButtonStyle}" Click="loadButton_Click" IsDefault="True" />

		<DataGrid x:Name="transactionDataGrid" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="10" Padding="5" IsReadOnly="True"/>




		<!--<Grid Grid.Row="3">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<ComboBox x:Name="fromTimePicker" Grid.Column="0" Margin="10,10,0,10" Padding="5" MinWidth="70" SelectionChanged="comboBox_SelectionChanged"/>
			<ComboBox x:Name="fromSlotPicker" Grid.Column="1" Margin="0,10,10,10" Padding="5" SelectionChanged="comboBox_SelectionChanged"/>
		</Grid>

		<Grid Grid.Row="3" Grid.Column="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<ComboBox x:Name="toTimePicker" Grid.Column="0" Margin="10,10,0,10" Padding="5" MinWidth="70" SelectionChanged="comboBox_SelectionChanged"/>
			<ComboBox x:Name="toSlotPicker" Grid.Column="1" Margin="0,10,10,10" Padding="5" SelectionChanged="comboBox_SelectionChanged"/>
		</Grid>-->
	</Grid>
</Page>
